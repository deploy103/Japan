# 개발일지

## 2026-05-27 작업 시작

### 작업 목표

- 한자 상세 뜻이 `face`처럼 영어로 노출되는 문제 수정
- 단어 분리/품사 표에 한국어 뜻 추가
- 계획서의 남은 기능을 실제로 사용할 수 있는 MVP 형태로 확장
- 기능 구현 내역과 다음 작업자가 이어받을 TODO를 계속 기록

### 현재 기준

- 서버: Express + Node 내장 SQLite
- 인증: 세션 쿠키 기반 로그인, 첫 관리자 계정 생성 스크립트 보유
- 현재 동작 중인 서버: `http://localhost:3000`

### 구현 내역

- `개발일지.md` 생성. 작업 목표, 현재 기준, TODO를 기록하기 시작했다.
- DB에 검색 기록, 단어장, 즐겨찾기, 퀴즈 시도, 오답노트 테이블을 추가했다.
- OpenAI API 키는 코드/문서에 넣지 않고 `.env`의 `OPENAI_API_KEY` 환경변수로만 읽도록 설정을 추가했다. 기본 모델은 공식 문서에서 최신으로 안내되는 `gpt-5.2`로 맞췄다.
- `src/services/japanese.js`에 OpenAI Responses API 기반 번역, 예문 생성, 이미지 OCR 호출 함수를 추가했다. 키가 없거나 호출 실패 시 기존 로컬 번역으로 fallback한다.
- 한자 뜻과 예시 단어 뜻은 한국어 매핑을 먼저 사용하고, 매핑이 없으면 `뜻 보강 필요`로 표시해 영어 뜻이 그대로 노출되지 않도록 수정했다.
- 단어 토큰에 `meaning` 필드를 추가해 단어 분리 표에 뜻을 붙일 수 있게 했다.
- 서버 API를 확장했다: `/api/dashboard`, `/api/vocabulary`, `/api/favorites`, `/api/examples`, `/api/ocr`, `/api/convert-kana`, `/api/quiz`.
- 분석 화면을 확장했다: 음성 입력/출력, OCR 이미지 업로드, 카메라 촬영 입력, 카나 변환, 조사 설명, 가타카나 분석, 문장 난이도, 문장 구조, 단어장, 검색 기록, 퀴즈, 오답노트, 학습 통계, 예문 생성, 다크모드.
- `.env`의 `OPENAI_API_KEY`를 읽어 실제 번역 호출을 검증했다. 샘플 `私は学生です。`가 provider `openai`로 `저는 학생입니다.`를 반환했다.
- 테스트에 단어 뜻/한자 한국어 뜻/카나 변환 검증을 추가했다.
- 실제 로그인 세션으로 `/api/analyze`, `/api/vocabulary`, `/api/quiz`, `/api/convert-kana`, `/api/examples`, `/api/dashboard`를 호출해 정상 응답을 확인했다.
- `/api/ocr`는 잘못된 이미지 입력에서 한국어 오류를 반환하는 것을 확인했다. 실제 이미지 OCR은 OpenAI Vision 호출이므로 실사용 화면에서 이미지로 추가 확인 필요.
- `npm test`는 OpenAI 비용/네트워크에 의존하지 않도록 `OPENAI_API_KEY=`로 비워서 실행되게 조정했다.
- 앱 첫 진입 시 샘플 문장 자동 분석이 검색 기록을 오염시키지 않도록 `saveHistory=false` 흐름을 추가했다. 사용자가 직접 분석, 음성 입력, OCR 분석을 실행한 경우만 기록된다.
- 한자 상세 패널에 한자 즐겨찾기 버튼을 추가했고, 학습 관리 영역에 즐겨찾기 목록을 렌더링하도록 보강했다.
- 입력 도구에 한자 상세 직접 검색을 추가했다. 문장에서 추출되지 않은 한자도 `/api/kanji/:char`로 조회해 같은 상세 패널에서 확인할 수 있다.
- 최신 서버 재시작 후 `saveHistory=false` 분석은 기록 수를 유지하고, `saveHistory=true` 분석은 기록 수를 1 증가시키는 것을 확인했다. 직접 한자 검색 `顔`도 한국어 뜻 `얼굴`로 반환된다.
- 로그인 후 `/app` HTML 렌더링에서 이미지 OCR, 카메라 촬영, 문법/난이도, 학습 관리, 예문 생성, 즐겨찾기, 다크모드, 한자 상세 검색 라벨이 모두 포함되는 것을 확인했다.
- 검증 과정에서 관리자 계정에 들어간 샘플 단어/퀴즈/오답/일부 검색 기록은 정리했다.
- 로그인/회원가입 rate limit을 15분 12회에서 40회로 완화했다. 개발/테스트 중 몇 번 틀렸다고 사용자가 막히는 느낌을 줄이기 위한 조정이다.
- 단어 분리 표에 JLPT 추정 컬럼을 추가했다. 단어 안 한자의 JLPT 중 가장 어려운 수준을 표시한다.
- 학습 통계의 오늘 분석 수를 SQLite UTC 기본 날짜가 아니라 localtime 기준으로 계산하도록 수정했다.
- OpenAI 예문 생성 응답이 JSON 코드블록으로 감싸져도 배열 부분을 추출해 파싱하도록 보강했다.
- 기본 대명사/동사(`彼`, `使う`, `買う`, `作る` 등) 한국어 단어 매핑을 추가해 단어 표의 `뜻 보강 필요` 노출을 줄였다.
- 경제/정치/사회/문화/역사/문법/한자/난이도 관련 기본 명사와 형용사 뜻 매핑을 추가했다.
- 세션 만료 상태에서 `/api/*`가 HTML 로그인 페이지로 리다이렉트되지 않도록 401 JSON을 반환하게 수정했다.
- 서버 통합 테스트에 비로그인 `/api/dashboard`가 401 JSON을 반환하는 검증을 추가했다.
- OCR/예문 생성 지연을 고려해 OpenAI 호출 타임아웃을 15초에서 30초로 늘렸다.
- 페이지 로드 시 샘플 문장을 자동 분석하지 않도록 변경했다. OpenAI 키가 설정된 상태에서 로그인/새로고침마다 번역 비용이 발생하지 않게 하기 위한 조치다.
- 클라이언트 렌더링에서 남아 있던 `innerHTML` 사용을 제거하고 DOM 생성 방식으로 통일했다.
- `npm audit --audit-level=moderate` 결과 취약점 0건을 확인했다.
- `.env`와 SQLite 데이터 파일을 제외한 작업 파일 목록을 점검했고, 키/비밀번호 원문 문자열이 없는 것을 확인했다.
- 19:31 KST 최종 점검: `npm test` 7개 통과, `/healthz` HTTP 200, 서버 PID `21984` 실행 중 확인.
- OCR 이미지 업로드는 base64 JSON 요청 크기를 고려해 클라이언트에서 6MB 이하만 허용하도록 제한했다.
- 운영 배포 확인용 `GET /healthz` 엔드포인트를 추가했다.
- 최신 서버를 PID `21984`로 재시작했고 `/healthz` HTTP 200 및 비로그인 `/api/dashboard` 401 JSON 응답을 확인했다.
- Ubuntu 배포용 예시 파일을 추가했다: `deploy/japanese-learning.service`, `deploy/nginx.conf.example`.
- 서버 통합 테스트 `tests/server.test.js`를 추가했다. 임시 SQLite DB와 임시 포트로 서버를 띄우고 회원가입, `/app`, 분석 API, 단어장 저장, 대시보드 통계를 검증한다.
- `npm test` 기준 총 7개 테스트가 통과한다. 통합 테스트는 단어장 저장 후 삭제까지 확인한다.
- 저장소 파일에서 OpenAI 키 원문이 노출되지 않는지 `rg`로 확인했다. 실제 키는 gitignored `.env`에만 있다.
- 즐겨찾기 목록의 item type을 `word/kanji/sentence`가 아니라 `단어/한자/문장`으로 표시하도록 수정했다.
- 한자 상세 API의 `meanings`도 한국어 우선 배열로 바꿨다. 원본 영어 뜻이 화면이나 API 기본 필드에 먼저 노출되지 않게 하기 위한 조치다.
- README의 OpenAI 키 예시도 `sk-...` 형태가 아니라 placeholder 문구로 바꿔 secret scan 오탐을 줄였다.
- `.env`를 제외한 저장소 파일에서 노출된 키/관리자 비밀번호 문자열이 없는 것을 다시 확인했다.
- 단어장과 즐겨찾기 목록에 삭제 버튼을 추가했다. 기존 DELETE API를 클라이언트에서 사용하도록 연결했다.
- 서버 통합 테스트에 단어장 DELETE 검증을 추가했다.
- 검색 기록 삭제 API `DELETE /api/history/:id`와 클라이언트 삭제 버튼을 추가했다.
- SQLite 운영 데이터를 파일로 백업하는 `npm run backup:db` 스크립트를 추가했다. 백업 파일은 `backups/`에 생성되고 git에는 포함하지 않는다.
- `npm run backup:db`를 실제 실행해 백업 파일 생성까지 확인했고, 검증용 백업 파일은 정리했다.
- `계획.md` 28개 기능을 다시 대조했다. 누락되어 있던 2번 `한국어 → 일본어 번역 기능`을 `/api/translate-ko-ja`, `translateKoreanToJapanese`, 메인 화면 입력 도구 UI로 추가했다.
- 한국어 → 일본어 번역은 OpenAI 키가 있으면 Responses API를 사용하고, 키가 없을 때 테스트 가능한 내장 예문 fallback을 사용하도록 구현했다.
- 한국어 → 일본어 번역 결과를 바로 일본어 분석 입력창에 넣고 분석할 수 있는 `분석창에 넣기` 버튼을 추가했다.
- OpenAI 비용이 발생할 수 있는 `/api/analyze`, `/api/translate-ko-ja`, `/api/examples`, `/api/ocr`에 별도 rate limit을 적용했다.
- API 404/500/본문 크기 초과 오류가 HTML이 아니라 JSON 오류로 내려가도록 에러 핸들러를 보강했다.
- OCR 서버 입력에도 data URL 크기 제한을 추가해 클라이언트 제한을 우회한 대용량 요청을 막도록 했다.
- 로그인/회원가입 직후 기존 세션 쿠키를 정리하고 새 세션을 발급하도록 조정해 세션 고정 위험을 줄였다.
- 정적 파일을 제외한 동적 응답에 `Cache-Control: no-store`를 적용했다.
- 개발용 고정 세션 시크릿 fallback을 제거하고, `SESSION_SECRET`이 없을 때는 프로세스별 랜덤 값을 쓰도록 바꿨다. 운영 환경은 기존처럼 32자 이상 고정 값을 요구한다.
- `src/server.js`, `src/services/japanese.js`, `src/db.js`에 유지보수용 주석을 추가했다.
- `npm audit --json` 기준 현재 설치 의존성 취약점은 info/low/moderate/high/critical 모두 0건이다.
- npm advisory/CVE DB를 보는 `npm audit` 기준 현재 설치 트리의 취약점은 0건이다. `express-rate-limit` 등 최신 major가 있는 패키지는 있으나, 보안 취약점이 잡힌 상태가 아니므로 이번 안정화에서는 동작 리스크가 작은 현재 major를 유지했다.
- 저장소 파일에서 OpenAI 키 원문, 관리자 비밀번호 원문, 실제 키 형태 문자열이 없는지 다시 검색했다. 실제 키는 `.env`에만 남아 있다.
- README를 현재 기능, 보안 설계, Ubuntu 26.04 배포, 검증, 파일 구조 중심으로 다시 작성했다.
- `npm test` 기준 총 8개 테스트가 통과한다. 새 테스트는 한국어 → 일본어 fallback, 서버 API 흐름, 잘못된 비밀번호가 요청 차단이 아니라 명확한 401 오류로 표시되는지를 검증한다.
- 추가 보안 패스에서 로그인/회원가입도 Origin 검사는 완화하되 CSRF 토큰은 다시 검증하도록 조정했다. 사용자가 겪던 출처 차단은 피하면서 auth POST의 CSRF 보호는 유지하기 위한 변경이다.
- 운영 환경의 Origin 검사는 `APP_ORIGIN`과 정확히 일치하는 출처만 허용하도록 강화했다. 개발 환경에서만 현재 요청 Host 기반 fallback을 허용한다.
- 운영 환경에서는 `SESSION_SECRET`이 없거나 32자 미만이면 서버가 시작되지 않도록 강화했고, `PORT` 범위와 `APP_ORIGIN` URL 형식도 시작 단계에서 검증하도록 추가했다.
- 비로그인 `/api/*` POST는 CSRF 오류보다 `401 로그인이 필요합니다.`가 먼저 보이도록 정리했다.
- `/healthz`는 세션/CSRF 쿠키를 굽지 않고, HTTP 개발 환경에서는 HSTS 헤더를 내리지 않도록 조정했다.
- `/api/convert-kana`에도 빈 입력/길이 제한 검증을 추가했다.
- 퀴즈 출제 API가 정답을 클라이언트로 함께 보내지 않도록 제거했다. 채점은 기존처럼 서버에서만 한다.
- `deploy/nginx.conf.example`에 `client_max_body_size 8m`, 프록시 타임아웃, forwarded header, healthz access log off를 추가했다.
- `deploy/japanese-learning.service`에 `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem`, `ProtectHome`, 제한된 `ReadWritePaths`를 추가했다.
- 다크모드에서 입력창 글자/배경 대비가 깨지지 않도록 input/textarea 배경을 테마 변수로 변경했고, 모바일 한국어→일본어 번역창 높이를 조정했다.
- SQLite 백업 스크립트가 WAL 모드 변경분을 놓치지 않도록 단순 파일 복사 대신 `VACUUM INTO` 기반 일관 스냅샷 백업으로 변경했고, 백업 후 passive WAL checkpoint를 수행하도록 했다.
- `npm run backup:db`를 다시 실행해 `backups/`에 새 SQLite 백업 파일이 생성되는 것을 확인했다.
- 유지보수용 npm 스크립트 `npm run check`, `npm run audit:security`를 추가했다. README의 검증 절차도 이 명령 기준으로 갱신했다.
- `npm outdated --long`으로 major 업데이트 후보를 확인했다. `dotenv 17`, `ejs 6`, `express 5`, `express-rate-limit 8`은 major 변경이므로 이번 안정화에서는 올리지 않았고, 현재 설치 트리는 `npm audit --omit=dev`에서도 취약점 0건이다.
- `npm audit signatures`로 설치 패키지의 npm registry signature도 확인했다. 87개 패키지 서명 검증, 1개 attestation 검증으로 통과했다. 이후 재검증용 `npm run audit:signatures` 스크립트도 추가했다.
- 추가 보안 점검에서 로그인 실패 메시지가 아이디 없음/비밀번호 오류를 구분해 계정 존재 여부를 노출할 수 있는 점을 수정했다. 이제 둘 다 동일한 401 메시지를 반환하고, 비활성 계정 상태는 비밀번호 검증 이후에만 안내한다.
- 운영 환경에서 `APP_ORIGIN`이 HTTPS가 아니거나 `COOKIE_SECURE=true`가 아니면 서버가 시작되지 않도록 강화했다.
- 비밀번호 최대 길이를 20자에서 128자로 완화해 긴 패스프레이즈를 사용할 수 있게 했다.
- `npm run check`, `npm test`, `npm run audit:security -- --omit=dev`, `npm run audit:signatures`를 재실행했다. 테스트는 총 10개 통과했고, npm audit 기준 취약점은 0건이다.

### TODO

- [x] 한국어 → 일본어 번역 API/UI 추가
- [x] 한자/단어 뜻 한국어 매핑 보강
- [x] 검색 기록, 단어장, 즐겨찾기, 퀴즈, 오답노트, 통계 DB 추가
- [x] 음성 인식/음성 출력 UI 추가
- [x] OCR 이미지 업로드/카메라 촬영 분석 추가
- [x] 히라가나/가타카나 변환, 가타카나 단어 분석, 조사 설명, 난이도/문장구조 분석 추가
- [x] 예문 생성, 즐겨찾기, 검색 기록, 학습 통계, 다크모드 UI 추가
- [ ] OCR 실제 이미지 검증
- [ ] Playwright 브라우저 렌더링 검증. 현재 로컬 Chromium 의존 라이브러리 문제(`libnspr4`)가 있으면 apt 의존성 설치 후 실행 필요
- [x] 테스트와 실제 서버 흐름 검증

### 파일 맵

- `src/server.js`: 라우팅, 인증, 세션, 관리자, 학습 API, 대시보드 API
- `src/db.js`: SQLite 스키마와 기본 PRAGMA
- `src/security.js`: 비밀번호 해시/검증, 입력값 검증, 토큰 유틸
- `src/services/japanese.js`: 형태소 분석, 양방향 번역, 한자 상세, OpenAI 번역/예문/OCR, 난이도/구조/조사 분석
- `src/services/koDictionary.js`: 한국어 뜻 매핑, 조사 설명, 가타카나 외래어 사전
- `public/app.js`: 분석 화면 클라이언트 로직, 음성/OCR/단어장/퀴즈/다크모드 UI 동작
- `public/styles.css`: 전체 UI 스타일과 반응형/다크모드 스타일
- `views/app.ejs`: 로그인 후 메인 학습 화면
- `views/admin.ejs`: 관리자 회원 관리 화면
- `scripts/ensure-admin.js`: 관리자 계정 생성/갱신
- `scripts/backup-db.js`: SQLite 파일 백업
- `tests/*.test.js`: 서비스, 보안, 서버 통합 테스트

### 남은 위험요소

- OpenAI 키가 채팅에 노출되었으므로 실제 운영 전에는 반드시 폐기하고 새 키로 교체해야 한다.
- OCR은 API와 UI가 붙었지만 실제 일본어 이미지로 최종 확인이 아직 필요하다.
- 브라우저 렌더링 검증은 로컬 Chromium 의존성 문제 때문에 아직 스크린샷으로 확인하지 못했다.
- 한국어 뜻 사전은 MVP 수준이다. 사용량이 늘면 `src/services/koDictionary.js`에 단어/뜻 매핑을 계속 보강해야 한다.
